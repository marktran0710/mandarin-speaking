# Python caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Secrets / local env
.env
.env.*

# Dev data must NOT be baked into the image — it lives on the /data volume.
mandarin_stories.db
*.db
*.sqlite3
uploads/

# Research/private corpus and derived reports must never be copied into a
# production image. They are local evaluation inputs, not application data.
private-data/
reports/

# Tests and dev-only tooling are not needed in the runtime image.
tests/
requirements-dev.txt
pytest.ini

# Local virtualenvs / logs
.venv/
venv/
*.log
