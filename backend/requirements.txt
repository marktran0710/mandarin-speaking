fastapi==0.104.1
uvicorn[standard]==0.24.0
numpy==2.4.3
scipy==1.17.1
pydantic==2.5.0
python-multipart==0.0.6
httpx==0.25.2
python-dotenv==1.0.0
PyJWT==2.10.1
bcrypt==5.0.0
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
alembic==1.14.0
sqlalchemy>=2.0
praat-parselmouth==0.4.7
opencc-python-reimplemented==0.1.7
jieba==0.42.1
pypinyin==0.52.0
edge-tts==7.2.8
soundfile==0.14.0
openpyxl==3.1.5
# Optional ML/ASR models — ctwhisper's deps (transformers/torch/librosa) are
# installed for the dev Docker image via requirements-local-asr.txt instead
# of being uncommented here, so a production build (Dockerfile.production,
# which never reads that file) can't accidentally pick them up.
# transformers>=4.51.3,<5.0.0
# torch>=2.3.0
# librosa>=0.10.0
