{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!(teacher|admin)\\.html$).*)",
      "destination": "/index.html"
    }
  ]
}
